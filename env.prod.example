# Threat-Forge Production Environment Configuration
# For DigitalOcean 1 CPU, 1GB RAM droplet

# Application Settings
ENVIRONMENT=production
DEBUG=false
SECRET_KEY=your-secret-key-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production

# Database Configuration
DATABASE_URL=postgresql+asyncpg://threatforge:your-postgres-password@db:5432/threatforge

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# CORS Settings (Replace with your actual domain)
ALLOWED_ORIGINS=http://your-domain.com,https://your-domain.com
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Rate Limiting (Reduced for low-resource environment)
RATE_LIMIT_PER_MINUTE=30
RATE_LIMIT_BURST=5

# File Upload Limits (Reduced for low-resource environment)
MAX_UPLOAD_SIZE=5242880  # 5MB
MAX_CSV_ROWS=10000

# Celery Configuration (Single worker for resource constraints)
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Threat Intelligence Provider API Keys
# Get these from the respective provider websites

# VirusTotal API Key (Recommended for basic functionality)
VIRUSTOTAL_API_KEY=your-virustotal-api-key

# URLScan.io API Key (Recommended for basic functionality)
URLSCAN_API_KEY=your-urlscan-api-key

# CrowdStrike Intel API Credentials (Optional)
CROWDSTRIKE_CLIENT_ID=your-crowdstrike-client-id
CROWDSTRIKE_CLIENT_SECRET=your-crowdstrike-client-secret

# Flashpoint API Key (Optional)
FLASHPOINT_API_KEY=your-flashpoint-api-key

# Recorded Future API Key (Optional)
RECORDED_FUTURE_API_KEY=your-recorded-future-api-key

# Logging
LOG_LEVEL=INFO
